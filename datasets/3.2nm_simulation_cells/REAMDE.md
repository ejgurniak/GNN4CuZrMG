###### simulation cells for boxes with box length L ~ 3.2 nm
- training data in './240each'
- Note: files are too large to compress all fold1, etc files in one folder
- data from each fold are separated into fold1_first_half, fold1_second_half, etc
- testing data in './target.tar.gz'
