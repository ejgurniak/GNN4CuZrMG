###### simulation cells for boxes with box length L ~ 4.4 nm
- training data in './240each'
- Note: files are too large to compress all fold1, fold2, etc.
- each fold is split into four folders to stay within GitHub limits
- testing data in './target.tar.gz'
